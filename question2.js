// Sum of Array //

//Find the sum of all elements using a loop.

let numbers = [10, 20, 30, 40, 50];

let sum = 0
for (let i = 0; i < numbers.length; i++) {
    sum = sum + numbers[i]
    
}
console.log ("sum:", sum);
